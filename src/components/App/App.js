import React from 'react';
import Layout from '../Layout/Layout';
import Contacts from '../Contacts/Contacts';

const App = () => {
  return (
    <Layout>
      <Contacts />
    </Layout>
  );
};

export default App;
